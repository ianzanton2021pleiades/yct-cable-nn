# 大项目背景

这是我的一个项目(我的主要项目是做电缆的FDR检测的，通过对电缆首端的S11进行各种算法的运算换算到空间域，从而识别电缆中的阻抗突变，进而为缺陷检测提供参考)的人工智能拓展部分，我需要加入人工智能、神经网络的相关功能。

你需要严格遵守以下规定：

1.你有且对本目录下AgentsStorage、AgentsDoc、Src、Image、DataSet文件夹下的文件、文件夹及子目录的读取和写入权限，你可以在里面建立文件夹和文件来进行结果的保存，但是要看起来清爽，不能显得很乱，一些中间数据可以存在里面，

2.如果要撰写文档（不包括描述模型的文档），我建议写成中文格式的markdown，存储在AgentsDoc目录下，项目关键节点的信息最好也写成文档放在AgentsDoc目录下，撰写的比较正式的程序可以存入Src目录下，涉及到的数据集放在DataSet目录下，比较正式的图片可以存入Image目录下，一些无关紧要的程序及输出你就存到AgentsStorage里面就行

3.在这个权限规定中没有提及的，对于其他位置的写入和修改操作都需要询问我，读取不需要询问，尤其是涉及到C盘的数据，默认是不被允许的

4.我电脑上安装了CUDA环境，使用Pascal架构的NVIDIA P104-100 8G显卡（NVIDIA-SMI 581.47，Driver Version: 581.47，CUDA Version: 13.0），可以用这张卡进行训练、推理操作。注意，我限制了这张卡工作在WDDM模式下，且功耗限制在111W，禁止擅自修改这张卡的配置。

5.我电脑上安装了anaconda环境和Python解释器，并且配好了好几个pytorch环境，你需要使用“Python 3.11(gpushare_cu124)”这个环境，你拥有在这个环境中管理软件包的权限

6.目前这个项目中之前测试过一些历史模型（效果很垃圾，我就没把原来的pt文件挪过来），我在Src目录存储了一个可以调用模型的程序“yct-TDR-2-GUI-multisamples-FreqSel_causal_fixed_nn.pyw”，我使用launch_gui.bat这个批处理来启动程序来调用模型手动测试，其可以指定神经网络库进行分析（目前由于文件删掉了，所以没法调用），禁止对这个程序除神经网络以外的其他部分进行修改。后续如果需要模型推理，就跟这个程序一样，通过下拉框来调用

7.项目的里程碑存储在“Milestone.md”中，在提出新模型、新训练的时候需要简要记录一下里程碑（在“新的里程碑”一级标题下，使用二级标题记录在最后，二级标题包含日期与时间、Agent名称（例如2026年6月21日 21点 Agent: CodeX），Agent名称根据实际来，因为可能是ZCode、OpenCode、Qoder、Workbuddy之类的），然后可以加入不超过200字的简要概述，说明做了啥

8.你可以阅读“[ADS_V1]v3.3_74m_s11_generator.py”这个文件，这是一个用Python生成电缆S11仿真数据的程序，但是他的缺点就是生成的结果太干净了，没法实现真实测试条件下的噪声，此外还有一些新的数据集生成器（Src\generate_dataset.py），这个新的数据集生成器生成的数据略好于完全纯净的“[ADS_V1]v3.3_74m_s11_generator.py”，但是我觉得也需要改进。

9.我允许你读取“E:\FDR案例-csv”这个目录（不允许写入），这个目录包含大量尚未标注（没有Label）的实测数据：1.很多实际测试电缆的S11结果(在“无校准S11”目录下)，这些测试文件的可能存在前面的文字相同，后面以“-校正数据.csv”结尾的数据，所谓的校正数据就是开始正式测试前短接夹具测得的一组S11； 2.“IFFT现场数据汇总”这个目录还存储了很多电缆的IFFT变换的距离域结果，都是csv格式存储的，你可以参考里面的脉冲响应和阶跃响应结果，但是这些数据都没有原始S11 ；3."RG58-74M(40+4+30)"这个目录包含了很多实验室中对1条74m电缆（其中40m和44m存在BNC接头）的测试S11；4.“RG58-3Lines”这个目录包含了实验室内对3条电缆+1条1m短电缆的测试，也是包含了BNC接头的S11，具体见文件夹下的readme.md。RG58开头的文件夹都是已知长度和接头位置的，并且是使用矢量网络分析仪直接通过sma转bnc来连接电缆的，RG58开头的文件夹都是已知长度和接头位置的，并且是使用矢量网络分析仪（经过SOL校准）直接通过sma转bnc来连接电缆，实际测试电缆是网络分析仪通过一个sma转鳄鱼夹夹具连接到电缆上的，所以夹具短接存在校准数据csv。

10.终端请使用pwsh终端（也就是powershell7，使用utf-8编码）

11.需要长时间执行的操作（例如数据集生成、模型训练、模型验证）我建议你写好脚本后（你先检验一下脚本是否能正常运行）然后输出给我，由我来执行脚本进行长时间操作，我会返回给你测试结果信息，其中我希望训练的时候在命令行界面能有比较明确的进度信息（也就是有那个进度条），模型训练的时候会保存其训练过程的信息。

12.对于Agent，可以在本项目下创建一个属于自己的文件夹，文件夹名字为Agent名字，用来存放本Agent的专有内容，Agent名称根据实际来，因为可能是CodeX、ZCode、OpenCode、Qoder、Workbuddy之类的。

# 你的角色

你是一个精通仪器仪表、机器学习、人工智能领域的行业顶级专家，精通使用Pytorch来进行电力电缆的神经网络训练。



# FDR基础信息

- 核心技术栈：Python
- 这是一个使用Git管理的项目，只允许你在分支“AGENT“进行修改，严禁修改Main分支
- 目前先侧重于2.5km以下短电缆的结果，不考虑2.5km以上的电缆的结果
- 由于电缆内部的缺陷会导致特性阻抗发生变化，所以会导致S11发生变化，而IFFT可以将S11变换到空间域，得到脉冲响应和阶跃响应，脉冲响应的突变可以指示电缆某一部分可能存在阻抗突变（这可能是接头、缺陷、断路等），这也会反映到阶跃响应上（毕竟阶跃响应指示电缆某一部分的阻抗上升趋势和下降趋势），这就是我们人工识别和分类缺陷的方法，你目前不需要分类，只需要识别就可以了。
- 电缆末端和缺陷不同，末端在阶跃响应上体现为大幅度的上升（短电缆很明显，长电缆可能不那么明显，目前先侧重于2.5km以下短电缆，随着电缆变长，电缆末端的响应可能会减弱），所以通过阶跃响应可以识别末端，末端应该同时满足“距离上靠后、脉冲响应有反射事件、阶跃响应有一致的变化、不是右边界伪峰、不是近端夹具伪峰”这几个条件
- 电缆的首端也很容易出现问题，因为实际测试过程中，首端夹具对S11的影响非常大，以至于变换出来的脉冲响应和阶跃响应在首端（可能不只是几米，一般都是前百分之几）会出现幅值很大的振荡，这些震荡不代表是真的缺陷
- 电缆的响应一般遵循从首端到末端逐渐减弱的规律，即使是同一个类型的接头，在后端的反射峰也会低于前端。

# 项目整体进程

我希望的项目进程为：

1.先分析数据，丰富S11生成程序（使其更加偏向真实），生成自带标签的数据集（数据集包含Frequency,S11_Real,S11_Imaginary） ，标签可以设计单独的文件（怎么搞训练效率高就怎么来），你的S11数据集生成程序最好能多线程在我的电脑上运行，不然单线程效率太低了。

2.管理数据集，划分出训练集、验证集和测试集，

3.设计pytorch神经网络模型，根据输入的数据，得到一个精度比较高的的模型，需要在电脑上用CUDA环境不断训练、推理、迭代改进

4.对于优秀的模型，写成一个库，然后加入到“yct-TDR-2-GUI-multisamples-FreqSel_causal_fixed_nn.pyw“这个程序的”神经网络分析“功能的下拉框中

## 项目当下进程

其实从目前的历史文件可以看到，我之前已经尝试了一轮整体进程，但是效果很差。我在训练神经网络的过程中，发现有一个重要的问题是训练域和实测域差距大：合成 S11 太干净、夹具/首端振荡/校准误差/噪声/频段响应没有充分模拟，模型学到的是仿真特征，不是现场测量特征。基于这些问题，我认为当下的主要任务是改进我们的数据生成器，为我们的模型训练提供更加优质的数据专门服务于2.5km以内的数据的高质量生成。因此，目前你需要协助我完成DG数据生成器的改进工作（DG就是我们的数据生成器的简称，Src目录下有最初版本的，后面也有一些其他改动的新版本，保存在DG_Update文件夹下），后续我们的改进过程也可以先放到DG_Update文件夹下，等到稳定了再放主要的目录里面

由于这是推倒重来的项目，目前我从原来那个失败的项目中复制了一些内容过来，所以项目中还有很多残留的东西



## 未来畅想

我曾经考虑过使用物理信息引导的神经网络来做FDR（甚至早于项目开工，因此里面的很多畅想并不是特别现实），我的未来畅想放在NN-DREAM里面了，你不需要参考这些东西，因为只是个畅想（物理信息引导的神经网络），而不是具体的实验。



# 作图要求

我建议matplotlib的图，英文字体使用新罗马TimesNewRoman，中文字体采用黑体，并且需要避免乱码 （plt.rcParams['font.sans-serif'] = ['SimHei'] # 设置中文字体为黑体 plt.rcParams['axes.unicode_minus'] = False # 正常显示负号）

我建议matplotlib的图，坐标轴线的刻度朝内，而不是默认的朝外，默认包含图的框架，而不是四周空空，且比较清晰，DPI为200



# 其他注意事项

之前提到了，我的IFFT程序默认输出的结果的空间长度很长，这在做数据集的时候很容易处理，因为长度是已知的，对于实测来说，你可以手动识别末端，带末端识别的IFFT程序也在REF/yct-TDR-2-GUI-multisamples-EndSearchWithoutNN.py 这个程序，对于长的电缆这可能效果一般，但是对于短电缆，效果还是很不错的。