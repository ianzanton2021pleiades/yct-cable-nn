# DG 2.5 km Dirty S11 数据生成器实施计划

**目标:** 新建 `Src/DG_dataset_max2.5km.py`，生成 2.5 km 内更接近 RG58 实验室和现场夹具测试形态的脏 S11 训练数据，并输出 8 条实测形态对比图。

**关键默认值:** 标签距离网格使用 `D_MAX=2500.0 m`、`DD=0.25 m`、`N_GRID=10000`。`DD=0.25 m` 是标签/模型输出网格间隔，不等于真实物理精度；CSV 距离域仍保留 IFFT 原始距离轴并截断到 `1.2 * cable_length`。

## 实施范围

- 创建 `Src/DG_dataset_max2.5km.py`。
- 默认输出到 `DataSet/DG_max2p5km/`。
- 生成 9 kHz-1 GHz/50000 点和 9 kHz-200 MHz/5000 点两份完整 Client 风格 CSV。1GHz 点数提高到 50000 是为了把频率步进压到约 20 kHz，从而覆盖 2.5 km 电缆的距离域范围。
- 生成同名 YAML 元数据、`.npy` 标签、`manifest.yaml`。
- preview 现在输出五张图：
  - Image/DG_max2p5km_realpart_comparison.png：包含 4 条 RG58 + 4 条现场实测的 S11 实部对比。
  - Image/DG_max2p5km_impulse_comparison.png：包含 4 条 RG58 + 4 条现场实测的脉冲响应对比（使用AI_TEST\Src\yct-TDR-2-GUI-multisamples-FreqSel_causal_fixed_nn.pyw里面的算法，使用Hann窗）。
  - Image/DG_max2p5km_step_comparison.png：包含 4 条 RG58 + 4 条现场实测的阶跃响应对比（使用AI_TEST\Src\yct-TDR-2-GUI-multisamples-FreqSel_causal_fixed_nn.pyw里面的算法，使用Hann窗）。
  - Image/DG_max2p5km_magnitude_comparison.png：包含同一批 8 条样本的 `20log10(|S11|)` 幅频特性对比。
  - Image/DG_max2p5km_phase_comparison.png：包含同一批 8 条样本的 VNA 风格包裹相位-频率特性对比。
  - 脉冲/阶跃图使用同一批 8 条样本，并且每个子图内三条曲线共用同一个归一化尺度，避免单独归一化误导幅值判断。
- 不修改 `Client`，不修改显卡配置，不写入 `E:\FDR案例-csv`。
- 本轮不适配训练端；当前 `Src/core/dataset.py`、`Src/train.py`、模型和评估脚本仍默认 `1200 m / 2400 点`，下一轮训练前必须单独改。

## 生成器接口

`Src/DG_dataset_max2.5km.py` 提供 CLI：

```powershell
python Src/DG_dataset_max2.5km.py `
  --output_dir DataSet/DG_max2p5km `
  --n_total 3000 `
  --seed 20260621 `
  --workers 4 `
  --profile mixed `
  --real_data_root E:\FDR案例-csv
```

参数要求：

- `--output_dir`: 默认 `DataSet/DG_max2p5km`
- `--n_total`: 默认 `3000`
- `--train_ratio`: 默认 `0.8`
- `--val_ratio`: 默认 `0.133`
- `--seed`: 默认 `20260621`
- `--workers`: 默认 `max(cpu_count - 1, 1)`
- `--profile`: `mixed | rg58 | field`
- `--real_data_root`: 默认 `E:\FDR案例-csv`
- `--preview_only`: 只生成 8 条实测形态对比图，不生成完整数据集

## 输出结构

```text
DataSet/DG_max2p5km/
  manifest.yaml
  raw/
    train/
      dg000000_1GHz.csv
      dg000000_200MHz.csv
      dg000000.yaml
    val/
    test/
  labels/
    train/
      dg000000.npy
    val/
    test/
```

CSV 六列固定为：

```text
Frequency,S11_Real,S11_Imaginary,Distance,ImpulseResponse,StepResponse
```

YAML 至少记录：

- `sample_id`
- `profile`
- `csv_1ghz`
- `csv_200mhz`
- `total_length_m`
- `epsr`
- `seed`
- `split`
- `sweep_1ghz`
- `sweep_200mhz`
- `label_grid: {d_max_m: 2500.0, dd_m: 0.25, n_points: 10000}`
- `defects`
- `joint_positions_m`
- `end_position_m`
- `dirty_params`

`manifest.yaml` 记录全局配置、划分数量、profile 分布、样本列表和生成时间。

## 生成逻辑

### 物理拓扑

- RG58 profile：
  - 长度主要分布在 `25-120 m`
  - 支持 `40+4+30 m`、`40+1+30 m`、`40+1+25+30 m` 等已知结构
  - BNC 接头位置写入 YAML，接头反射进入标签的 `joint_positions_m`
  - 噪声低，频响起伏轻，首端夹具污染弱

- Field profile：
  - 长度主要分布在 `100-2500 m`
  - 文件夹名中的近似长度可作为 preview 复现长度参考
  - 允许 0-3 个缺陷段，缺陷段长度和阻抗失配保持保守
  - 首端强振荡、夹具短接残留、低频偏置、频响起伏比 RG58 更明显

- Mixed profile：
  - 默认 35% RG58、65% Field
  - profile 写入每个样本 YAML 和 manifest

### Dirty 污染模型

在干净多段传输线 S11 后叠加以下污染：

- 复高斯噪声：低频略强，中高频稳定，RG58 小于 Field。
- 乘性幅相扰动：平滑随机曲线，不做尖锐非物理扰动。
- 首端夹具振荡：Field profile 中加入短延迟多反射项，主要影响前若干米距离域。
- 校正残留：读取现场 `*-校正数据.csv`，提取平滑形态作为夹具参考，只用于污染模板，不当作标签数据。
- 接头反射：按距离衰减，避免远端接头峰不合理地强于近端。
- 频率响应误差：Field profile 加入轻微频率相关幅度衰减和相位偏移。

## 实测读取与 Preview 对比

读取 `E:\FDR案例-csv`，跳过 `IFFT现场数据汇总`。

兼容列名：

- 频率列：包含 `freq`
- 实部列：包含 `real`
- 虚部列：包含 `imag`

RG58 数据源：

- `E:\FDR案例-csv\RG58-74M(40+4+30)`
- `E:\FDR案例-csv\RG58-3Lines`

现场数据源：

- `E:\FDR案例-csv\无校准S11`
- 排除 `*-校正数据.csv` 作为待复现实测主文件
- 同目录校正文件只作为夹具污染参考

Preview 行为：

```powershell
python Src/DG_dataset_max2.5km.py --preview_only --seed 20260621 --real_data_root E:\FDR案例-csv
```

输出Image目录下的三张图：

对于S11对比的图像要求：

- 2x4 子图
- 每张子图包含：
  - 实测 S11 实部
  - 1 GHz 模拟 S11 实部
  - 200 MHz 模拟 S11 实部
- matplotlib 设置：
  - 英文字体 Times New Roman
  - 中文字体 SimHei
  - `axes.unicode_minus=False`
  - tick 朝内
  - DPI 200

## 实施步骤

1. 建立 `DG_dataset_max2.5km.py` 的 CLI、常量和路径处理。
2. 实现兼容实测 CSV 读取函数，支持 RG58 和现场列名。
3. 实现 `SweepConfig` 双频段配置：
  - `9e3-1e9, 50000`
   - `9e3-200e6, 5000`
4. 实现 2.5 km 内随机电缆拓扑生成：
   - RG58 profile
   - Field profile
   - Mixed profile
5. 实现 dirty 污染函数：
   - RG58 低污染
   - Field 夹具污染
   - 校正数据模板抽取
6. 实现单样本生成：
   - 同一拓扑分别生成 1 GHz 和 200 MHz S11
   - 分别 IFFT 得到距离域
   - CSV 距离域截断到 `1.2 * total_length`
   - 标签 `.npy` 使用 `2500 m / 0.25 m`
7. 实现多进程生成：
   - 每个样本独立 seed
   - worker 只写本样本文件
   - 主进程统一写 `manifest.yaml`
8. 实现 preview：
   - 抽取 4 条 RG58 + 4 条现场样本
   - 生成形态对齐模拟曲线
   - 保存实部对比组图
9. 加入终端进度输出：
   - 数据集生成显示样本进度、速度、ETA
   - preview 显示选中文件路径和输出图路径

## 验证计划

静态检查：

```powershell
python -m py_compile Src/DG_dataset_max2.5km.py
```

小样本生成：

```powershell
python Src/DG_dataset_max2.5km.py `
  --output_dir DataSet/DG_max2p5km_smoke `
  --n_total 12 `
  --seed 20260621 `
  --workers 2
```

格式验证：

- 每个样本存在：
  - `*_1GHz.csv`
  - `*_200MHz.csv`
  - `.yaml`
  - `.npy`
- `*_1GHz.csv` 有 50000 个有效频率点。
- `*_200MHz.csv` 有 5000 个有效频率点。
- 两份 CSV 都包含六列 Client 格式。
- CSV 距离域最大值不超过 `1.2 * total_length` 附近的最后一个 IFFT 点。
- `.npy` shape 为 `(10000,)`。
- YAML 中 `label_grid.dd_m == 0.25`，`label_grid.d_max_m == 2500.0`。

Preview 验证：

```powershell
python Src/DG_dataset_max2.5km.py `
  --preview_only `
  --seed 20260621 `
  --real_data_root E:\FDR案例-csv
```

检查：

- `Image/DG_max2p5km_realpart_comparison.png` 存在。
- 图中 8 个子图均非空。
- RG58 子图的接头/末端振荡位置大体合理。
- Field 子图体现首端夹具污染和频域起伏，但不宣称精确反演。

## 风险与明确不做项

- `DD=0.25 m` 只是标签网格更细，不保证现场真实定位精度达到 0.25 m。
- `9 kHz-1 GHz / 50000点` 的频率步进约 20 kHz，距离覆盖能力明显强于 10000 点版本；脚本仍必须在 YAML/manifest 记录 `band_distance_coverage` 和 `truncated_by_ifft_range`，防止个别高介电常数或边界样本被误读。
- `9 kHz-200 MHz / 5000点` 的频率步进约 40 kHz，更适合作为 2.5 km 范围内的距离域覆盖参考。
- 现场数据无标签，preview 只做形态对齐，不输出“真实缺陷位置”。
- 训练端当前仍是 `1200 m / 2400 点`，本轮生成的 `2500 m / 10000 点` 标签不能直接无缝用于旧训练脚本。
- 不修改 `Src/core/dataset.py`、`Src/train.py`、模型结构、GUI 或 `Client`。
- 不对 `E:\FDR案例-csv` 写入任何文件。

## 2026-06-21 质量修正记录

- 三张 preview 图必须使用同一批 8 条案例，分别检查 `S11` 实部、IFFT 脉冲响应、IFFT 阶跃响应，不能只凭频域实部判断生成质量。
- RG58 高频实部不应出现模型人为引入的周期性“噪声包络”。重点约束 BNC/接头反射项和首端夹具项的高频频率响应，避免常幅正弦反射贯穿高频。
- RG58 脉冲响应应保留前 10 m 首端不匹配/夹具过渡造成的轻微振荡，但 BNC 接头和末端开路峰不应明显高于实测。

## 2026-06-22 调试与验收补充

- Preview 必须始终生成三张组图，且三张图必须来自同一批 4 条 RG58 + 4 条现场样本：
  - `Image/DG_max2p5km_realpart_comparison.png`
  - `Image/DG_max2p5km_impulse_comparison.png`
  - `Image/DG_max2p5km_step_comparison.png`
- 每次运行 `--preview_only` 时，终端必须输出可检查的量化指标：
  - RG58：前 10 m 脉冲响应峰值、BNC/接头窗口峰值、末端窗口峰值，并给出 DG/实测峰值比。
  - RG58：200-400 MHz、400-1000 MHz 实部快速振荡 P95，约束 DG 高频周期包络不能显著大于实测。
  - Field：目录标称长度、1GHz 估计末端、200MHz 估计末端、若按目录长度反推的 `epsr`。
  - Field：1GHz 与 200MHz 的脉冲/阶跃基线 P95，约束 200MHz 基线不能大到掩盖末端事件。
- 现场电缆 preview 不把随机缺陷当真实标签；除非实测文件名或用户给出明确缺陷位置，否则 preview 只拟合首端夹具、传播衰减、末端响应和整体频域形态。
- RG58 高频周期包络优先从远端强相干反射、频率无关接头项、末端开路过强三个来源排查；不得简单通过提高白噪声掩盖。
- Field 长度偏差优先从 `epsr` 假设、目录近似长度、终端类型和 IFFT 末端识别窗口排查；需要在图标题和终端日志中同时展示目录长度与估计长度。
- 200MHz 数据不能另行生成一个独立脏电缆；必须由同一个物理 S11 低频段派生，但允许使用频段相关的仪器/夹具残留衰减模型，避免低频 IFFT 基线被非物理放大。

## 2026-06-22 幅频与相频新增检查要求

- 在已有 `S11` 实部、IFFT 脉冲响应、IFFT 阶跃响应三张 preview 组图之外，新增两张同批样本组图：
  - `Image/DG_max2p5km_magnitude_comparison.png`
  - `Image/DG_max2p5km_phase_comparison.png`
- 幅频图使用 `20log10(|S11|)`，用于检查整体频响包络、低频/高频幅度衰减和非物理幅度台阶。
- 相频图使用由 `S11_Real/S11_Imaginary` 计算的相位，即 `atan2(Im(S11), Re(S11))`；preview 显示 VNA 风格包裹相位，单位为 degree，范围约为 `[-180, 180]`，用于直接对比实测仪器导出的相位锯齿形态。展开相位只保留在 `--diagnose_preview` 的相位斜率/残差指标中，用于数值诊断传播延迟和异常漂移。
- 幅频和相频应与原始实测结果保持同类形态，但约束强度低于 `S11` 实部、IFFT 脉冲响应和阶跃响应；不能为了贴合幅相图破坏前三类核心诊断图。
- 五张 preview 图必须使用同一批 4 条 RG58 + 4 条现场样本、同一批模拟结果，避免不同图之间样本漂移导致误判。

## 2026-06-22 相频趋势修正记录

- 当前相频偏差主要表现为展开相位的低阶趋势和斜率不一致，不是相位计算公式错误；相位来自 `atan2(Im, Re)`，不是 `Re/Im`。
- 修正策略：在 `apply_measured_template_shape()` 之后增加弱相位趋势约束，只读取实测模板的平滑展开相位趋势，并移除每条曲线的初始相位偏置。
- 该约束不混合高频相位残差，且限制单次相位修正幅度，避免为了贴合相频图而制造或移动 IFFT 脉冲/阶跃事件。
- 相位趋势约束只对 Field profile 启用，因为验证中发现 RG58 的接头/末端事件对平滑相位旋转过于敏感；RG58 继续优先保证 `S11` 实部、脉冲响应和阶跃响应。

## 2026-06-22 短 Field 样本修正记录

- 新 seed `20260622` 抽到的 `250-300 m` 现场样本暴露出短 Field 分支问题：幅频高频段被压得过低，相位高频残差过大，且 `170-270 m` 末端前区间出现非实测伪振铃。
- 根因：短现场电缆仍沿用长 Field 的弱开路终端、强高频衰减、较长夹具残留距离和较强相位趋势约束；这些假设对 `1-2 km` 样本可以接受，但会把短电缆的强端部反射和高频能量错误地压低，再通过相位补偿转化为距离域伪振荡。
- 修正：Field 按长度分层，`<500 m` 样本启用短 Field 参数：
  - 开路终端负载提高到更强反射范围，不再使用长 Field 的 `55-120 Ω` 近 50 Ω 弱开路。
  - 高频损耗和色散强度降低，保留短电缆 `400-1000 MHz` 的幅频能量。
  - 夹具/钳夹污染的距离范围和幅值降低，减少末端前中距离伪振铃。
  - 短 Field 禁用相位趋势约束，避免把模板高频相位残差注入到 IFFT 中；相频只保留生成器自身的传播相位和低强度幅频模板约束。
- 该修正只改变 Field 生成器；RG58 分支不改，长 Field 的相位/距离域约束保持原策略。
- 撤销说明：曾尝试加入短 Field 专用宽带近端夹具残留项来改善相频，但它会明显改变 S11 实部和距离域近端背景，违反“S11 实部、脉冲响应、阶跃响应优先”的约束，因此不采用。
- 末端阶跃修正：诊断显示短 Field 阶跃末端偏弱主要来自末端脉冲面积/宽度不足，而不是单点峰值不足；例如 250-300 m 样本实测末端宽度可达约 `10-30 m`，DG 原始末端更接近窄脉冲。当前只对 `<500 m` Field 增加低通末端尾部展宽，目标是适度增加末端面积并增强阶跃抬升；不得作用于长 Field，避免破坏 1700 m 样本阶跃形态。
- 当前判断：末端阶跃偏弱应优先从末端脉冲的“面积/宽度”解释，而不是只看单点峰值；如果脉冲末端过窄，即使局部峰值不低，积分后的阶跃末端仍会偏弱。后续优化应优先让末端事件在不抬高中距离基线的前提下适度变宽、增加面积。
- 当前限制：末端尾部展宽只能作为保守修正；如果继续追求更宽的实测末端形态，必须先建立硬门限，保证 `S11` 实部、末端前基线、脉冲响应和阶跃响应不劣化，再考虑更强的频率相关终端/介质色散模型。
- 优先级原则：`S11` 实部、IFFT 脉冲响应、IFFT 阶跃响应是数据生成质量的主约束；幅频和相频只能作为次级形态约束。任何为了改善幅频/相频或末端展宽而导致核心三图变差的复数叠加、相位旋转或宽带近端项，都应撤销或降级。

## 2026-06-22 已确认根因与修正方向

- RG58 高频周期包络的主要风险来自近端/接头反射项的高频包络过宽，以及把实测快速残差直接当模板注入。修正方向：RG58 preview 只混合实测慢变包络，快速模板注入保持为 0，并压低近端夹具项在 200 MHz 以上的包络。
- 现场电缆此前不相似的主要原因不是白噪声过强，而是传播衰减范围不合理：`0.05-0.16 dB/m @100MHz` 会把 1-2 km 末端事件衰减到过弱，随后又需要非物理补偿。修正方向：现场健康段改用低得多的长电缆衰减范围，同时把末端开路建模为经过长距离和夹具后的非理想等效开路。
- 现场 200 MHz 脉冲/阶跃失真的主要风险来自低带宽截断后相干末端项过硬，导致 IFFT 预振铃和阶跃大尺度弯曲。修正方向：200 MHz 仍从同一 1 GHz 脏 S11 派生，但加入保守的低带宽校准衰减，目标是让 200 MHz 基线和末端峰落在实测同量级。
- 4 条现场 preview 的末端估计显示 1 GHz 与 200 MHz 基本一致，因此“长度对不上”的主要来源是目录标称长度和 `epsr` 假设，而不是生成器两个频段不一致。特别是 `某地-2000m` 样本若按目录 2000 m 解释，需要 `epsr≈3.02`，不能强行按 `epsr=2.3` 画到 2000 m。

## 2026-06-22 低频与首端偏置修正记录

- Field profile 中 `DG200MHz` 曾经被整体乘以 `0.32`，这会导致同一条电缆在 200 MHz 内 `DG200MHz` 与 `DG1GHz` 的 `Re(S11)` 不一致。该全局缩放已移除；200 MHz 现在只由同一条 1 GHz 脏 S11 插值得到。
- 低频 `Re(S11)` 幅值偏低不能靠直接叠加慢趋势解决，因为慢趋势/DC 分量会被 IFFT 阶跃积分成整体抬高。当前修正为：实测模板分解为很慢基线与低频交流起伏，低权重混合基线，高权重混合低频交流起伏。
- 首端脉冲几乎全为正的根因是近端夹具/多反射项使用固定正幅值。当前修正为：近端项使用交替符号和小复相位扰动，使首端 0-10 m 出现正负混合振荡。
- 为缓解阶跃整体偏高，现场开路/短路末端被建模为更接近 50 Ω 的非理想等效终端，避免低带宽下远端事件过硬。
- 现场电缆目录名长度和 IFFT 估计长度可能不一致。Preview 阶跃/脉冲图应根据实测 IFFT 响应估计有效末端位置，并在图中标注估计长度，不能盲信文件夹名。
- 200 MHz sweep 的距离覆盖更好但带宽较低，脉冲主瓣天然更宽；preview 中不能把 200 MHz 脉冲尖峰宽度和 1 GHz 等价比较，也不能让 200 MHz 基线振荡大到掩盖主要反射事件。
- 调整过程必须保留量化依据，例如按频段比较 `Re(S11)` RMS/95% 幅值、统计实测阶跃/脉冲估计末端，避免通过简单拉高噪声幅值来“贴图”。

## 2026-06-23 诊断入口与二次形态修正记录

- 新增 `--diagnose_preview` 只读诊断入口，复用与 preview 图完全相同的 4 条 RG58 + 4 条 Field 样本和同一批 DG 曲线，输出 JSON 指标：实部 P95、快速实部 P95、幅频 dB 差、相位斜率/残差、近端/接头/末端脉冲峰和阶跃末端增量。
- RG58 分支降低 74 m 实验室电缆的等效高频损耗，提高有限开路末端范围，并新增实测幅频能量下限门控；`seed=20260622` 下 `400-1000 MHz` 快速实部比值中位数由约 `0.29` 提升到约 `1.31`，幅频 P95 差中位数由约 `30.8 dB` 降到约 `11.0 dB`。
- RG58 近端夹具项改为更强但更低通的 0-10 m 多反射，用于改善首端振荡，不再靠提高高频白噪声掩盖频域差异。仍需注意个别 Shield 样本末端/近端比例偏高，后续应继续用 `--diagnose_preview` 监控。
- 短 Field (`<500 m`) 末端不再依赖窄尖峰增强：先弱化短 Field 的窄开路负载，再加入更低带宽、更宽距离范围的末端面积项。`seed=20260622` 下 Field 阶跃末端增量比值中位数约为 `0.62`，200 MHz 约为 `0.66`，短样本末端峰不再系统性达到 `3-4x`。
- 短 Field 相位色散强度进一步降级，避免高频相位趋势修正转化为末端前伪振铃；长 Field 仍保留弱趋势约束。
- 这轮修正没有解决所有幅相差异：Field `400-1000 MHz` 幅频仍偏低，高频快速实部仍偏弱。当前取舍是优先保证 `S11` 实部、脉冲和阶跃的训练可用性，不为贴合高频幅相破坏核心三图。

## 2026-06-21 模板约束重构记录

- Dirty 模型已从纯随机扰动扩展为“物理 S11 + 实测模板约束”的混合生成方式。RG58 和现场实测 CSV 只作为频域包络、残差形态和夹具污染参考，不作为有标签训练样本。
- 新增实测模板字段：
  - dataset 生成时按 profile 随机抽取 `measured_template_source`；
  - preview 复现时使用对应实测 CSV 作为模板约束，使图像检查更接近“同类形态能否复现”。
- 高频异常修正：
  - 新增 `highfreq_decay_strength` 和 `event_hf_damping`；
  - 按实测模板的 `Re(S11)` 分频段 95% 幅值限制快速振荡能量，避免 200 MHz / 400 MHz 之后模拟振荡幅值超过实测；
  - 下调 field profile 的远端随机反射项，删除造成距离域孤立尖峰的 60-700 m 虚拟反射。
- 色散修正：
  - 保留频率相关相位色散；
  - 增加强长度相关高频衰减，使长电缆高频响应不再像短 RG58 一样保持过强振荡。
- 双频段一致性修正：
  - `*_1GHz.csv` 和 `*_200MHz.csv` 改为同一 dirty realization；
  - 200 MHz S11 由 1 GHz dirty S11 插值到 `9 kHz-200 MHz / 5000` 点，不再独立重新随机生成污染。
- 现场 200 MHz 修正：
  - field profile 的 200 MHz 分支加入低频慢变残留抑制，降低 IFFT 后阶跃响应被基线漂移主导的风险。
- 末端负载修正：
  - field open 末端由弱失配 `80-260 Ω` 改为有限高阻开路 `320-2200 Ω`，避免开路现场样本末端阶跃过弱。
- 已验证：
  - `python -m py_compile Src/DG_dataset_max2.5km.py`
  - `--preview_only` 生成 `realpart`、`impulse`、`step` 三张 2x4 对比图
  - `n_total=12` smoke 数据集生成成功
  - 结构校验通过：`1GHz=50000` 点，`200MHz=5000` 点，CSV 六列完整，标签 shape 为 `(10000,)`

## 2026-06-21 返工调试门禁

- 本轮目标不是“看起来更脏”，而是让 `S11` 实部、IFFT 脉冲响应、IFFT 阶跃响应同时接近 4 条 RG58 与 4 条现场实测案例。
- 每次重要调整后必须生成并人工检查三张固定组图：
  - `Image/DG_max2p5km_realpart_comparison.png`
  - `Image/DG_max2p5km_impulse_comparison.png`
  - `Image/DG_max2p5km_step_comparison.png`
- 三张图必须使用同一批 8 条案例、同一批模拟结果，不能用只优化其中一张图的方式掩盖其他域的问题。
- 本轮重点排查并记录：
  - RG58 高频实部周期性包络的来源，尤其是模板残差、接头项、末端项或相位/色散项是否人为制造了实测中不存在的周期性包络。
  - 4 条现场案例的有效末端距离，分别统计 `<=1GHz` 与 `<=200MHz` 的 IFFT 估计末端，并评估 epsr 假设对距离轴的影响。
  - RG58 前 10 m 首端不匹配振荡、BNC 接头峰、末端开路峰的相对幅值，避免接头和末端峰明显高于实测。
  - field profile 的 1 GHz 与 200 MHz 脉冲响应基线，尤其禁止 200 MHz 模拟基线大到掩盖主要反射事件。
- 调整过程中的关键判断必须写回本计划，尤其是发现某个模型项导致错误形态时，要记录“根因 -> 修改 -> 验证结果”。

## 2026-06-21 返工诊断与修正记录

- RG58 高频周期性包络根因：
  - 定量检查发现上一版模拟 RG58 的末端峰相对前 10 m 近端峰过强，`end/near` 约为 `3.4-14`，而当前 4 条实测 RG58 的 `end/near` 约为 `0.17-1.49`。
  - 这不是普通噪声，而是过强的相干末端/接头反射在频域形成规则周期包络。
  - 修正：降低 RG58 有限开路负载范围，收窄 BNC/缺陷段阻抗失配，提高 RG58 高频损耗，禁用 RG58 preview 的快速实测残差混入，仅保留实测幅值约束。
  - 修正后 RG58 模拟 `end/near` 降到约 `0.58-1.55`，前 10 m 首端不匹配震荡明显增强，末端峰不再系统性压过近端峰。
- field 距离重估：
  - 白塔寺 C 相：目录 `1350 m`，`<=1GHz` 估计 `1297.7 m`，`<=200MHz` 估计 `1299.7 m`，若强行按目录长度解释，等效 `epsr≈2.13`。
  - 某地 1500 m C 相短路：目录 `1500 m`，`<=1GHz` 估计 `1467.1 m`，`<=200MHz` 估计 `1468.3 m`，等效 `epsr≈2.20`。
  - 白塔寺 A 相：目录 `1350 m`，`<=1GHz` 估计 `1329.7 m`，`<=200MHz` 估计 `1330.4 m`，等效 `epsr≈2.23`。
  - 某地 2000 m A 相：目录 `2000 m`，`<=1GHz` 估计 `2291.5 m`，`<=200MHz` 估计 `2293.3 m`，若按目录长度解释则等效 `epsr≈3.02`；该案例更可能是长度标注/介电常数假设不一致，不能简单按 `2000 m` 画末端线。
  - Preview 标题已同时显示目录长度、1GHz 估计长度、200MHz 估计长度和等效 epsr。
- impulse / step 基线修正：
  - field 200MHz 之前用“慢变相减”处理低频残留，会把低频误差积分成更严重的阶跃漂移；这是错误方向。
  - 修正：移除 field 200MHz 慢变相减，改为同源 1GHz dirty S11 插值后做保守幅值缩放，优先避免 200MHz 脉冲/阶跃基线掩盖主要反射事件。
  - RG58 前 10 m 首端不匹配由多个近端低通反射项模拟，BNC/末端远端相干项被降低并增强频率衰减。
- 本轮验证：
  - `python -m py_compile Src/DG_dataset_max2.5km.py` 通过。
  - `python Src/DG_dataset_max2.5km.py --preview_only --seed 20260621 --real_data_root E:\FDR案例-csv` 通过，并生成三张组图。
  - `python Src/DG_dataset_max2.5km.py --output_dir DataSet\DG_max2p5km_smoke --n_total 12 --seed 20260621 --workers 2 --real_data_root E:\FDR案例-csv` 通过。
  - 结构校验通过：12 个样本，`1GHz=50000` 点，`200MHz=5000` 点，CSV 六列完整，标签 shape 为 `(10000,)`。

## 2026-06-21 返工二次收敛记录

- RG58 随机短电缆分支也同步采用更强高频损耗和有限开路负载，避免 dataset 中非 preview 的 RG58 样本仍保留过强远端相干反射。
- RG58 高频规则包络的主要处理策略保持为：
  - 不再把单条 RG58 实测快速残差直接混入 preview；
  - 通过实测幅值上限约束快速振荡；
  - 用近端低通多反射模拟前 10 m 首端不匹配；
  - 对远端 BNC/末端事件增强频率衰减。
- field `200MHz` 分支进一步降为同源 `1GHz` dirty S11 的保守缩放版本，缩放系数从 `0.34` 收敛到 `0.24`，目标是降低 200 MHz IFFT 后的脉冲/阶跃基线污染。
- 最新验证：
  - `py_compile` 通过。
  - `--preview_only` 通过并重生成三张组图。
  - `n_total=12` smoke 生成通过。
  - 结构校验通过：`1GHz=50000`、`200MHz=5000`、标签 `(10000,)`。

## 2026-06-21 Preview 拓扑隔离记录

- field preview 以前直接调用随机 field 拓扑，会在无标签现场样本中随机插入缺陷段，导致脉冲响应中出现不属于该实测案例的中间尖峰。
- 修正：`make_field_cable()` 增加 `n_defects_override`，dataset 生成仍按原策略随机缺陷；preview 复现现场样本时强制 `n_defects_override=0`，只检查夹具污染、传播衰减、末端响应和基线形态。
- 这不是削弱训练数据，而是隔离 preview 诊断目标：现场样本没有可靠缺陷标签，复现图不应该用随机缺陷去猜中间事件。
- 验证结果：
  - `py_compile` 通过。
  - `--preview_only` 通过，三张图已重生成。
  - `n_total=12` smoke 通过。
  - 结构校验通过：12 个样本、双频点数和标签 shape 均正确。

## 2026-06-23 Field/RG58 缺陷数量分布策略

- Field profile 不再使用均匀 `0-3` 缺陷采样；改为长度分层概率采样，最大缺陷数为 `5`。
- Field 推荐概率表：
  - `<80 m`: 固定 `0`。
  - `80-250 m`: `0/1/2/3/4/5 = 45/35/17/3/0/0%`。
  - `250-500 m`: `28/34/24/10/3/1%`。
  - `500-800 m`: `22/31/27/13/5/2%`。
  - `800-1500 m`: `16/28/29/17/7/3%`。
  - `1500-2500 m`: `12/24/29/21/10/4%`。
- 该分布让健康、单缺陷、双缺陷仍为主体，同时在 `500 m` 以上稳定保留 `4-5` 缺陷能力，避免长电缆训练集完全缺少多事件样本。
- `rg58_random` 与 Field 分开处理，只允许低概率 `1-2` 个本体缺陷：
  - `10-50 m`: `0/1/2 = 70/25/5%`。
  - `50-120 m`: `58/32/10%`。
  - `120-200 m`: `50/35/15%`。
- 已知 RG58 模板继续保持实验室拓扑语义，不额外注入随机本体缺陷；BNC/拼接事件仍通过 `joint_positions_m` 表达。
- Field preview 继续强制 `n_defects_override=0`，因为现场 preview 没有真实缺陷标签，不能把随机缺陷画成拟合结果。
- YAML/manifest 应记录 `defect_count_policy`，便于回溯数据集生成版本和实际缺陷数来源。

## 2026-06-23 Field 长距离老化/受潮缺陷策略

- 仅对 Field profile 增加长距离缺陷；已知 `rg58` 模板和 `rg58_random` 继续保持短缺陷/接头语义，不生成 `aging` 或 `moisture`。
- 缺陷总数仍由 `defect_count_policy()` 决定；随后按概率把其中少数缺陷替换为长距离缺陷。
- 长距离缺陷类型：
  - `short`: 现有短距离阻抗突变缺陷，仍为主体。
  - `aging`: 长距离老化段，表现为温和阻抗漂移、损耗升高和轻微波速变化。
  - `moisture`: 长距离受潮段，表现为更明显 `epsr` 升高、损耗升高、波速下降和响应延长。
- Field 每个缺陷被替换为长距离缺陷的概率：
  - `<250 m`: `0%`。
  - `250-500 m`: `3%`。
  - `500-800 m`: `6%`。
  - `800-1500 m`: `10%`。
  - `1500-2500 m`: `16%`。
- 长缺陷数量限制：`<800 m` 最多 `1` 个，`>=800 m` 最多 `2` 个；空间不足时降级为 `short`，不强行挤压健康段。
- 长缺陷类型比例：`aging/moisture = 60/40`。
- 长段长度模型：
  - `aging`: `clip(total_length * U(0.05, 0.16), 20 m, 320 m)`。
  - `moisture`: `clip(total_length * U(0.03, 0.10), 15 m, 220 m)`。
- 物理参数：
  - `aging`: `z0` 变化约 `±2-8%`，`epsr` 增加 `0.05-0.25`，`alpha` 放大 `1.4-3.0x`。
  - `moisture`: `z0` 变化以 `-3% 到 -12%` 为主，`epsr` 增加 `0.20-0.80`，`alpha` 放大 `2.0-6.0x`。
- YAML `defects` 增加 `type/start_m/end_m/center_m/alpha_db_per_m_100mhz` 等字段；`manifest.yaml` 记录 `defect_type_policy`。
- `.npy` 仍为单通道异常置信度：`short` 保持中心高斯峰，`aging/moisture` 使用区间标签和两端平滑过渡，不改训练端模型结构。

## 2026-06-23 受潮阶跃与长电缆相频修正记录

- PDF 依据显示：受潮会使等效介电常数和介质损耗增大，表现为 `C/G` 增大、特性阻抗下降、传播损耗增强和波速降低；阶跃响应应体现局部下沉、平台下移或从受潮起点开始缓慢下降，而不是硬边界矩形槽。
- `moisture` 不再作为生成时的明确类型；Field 受潮拆分为：
  - `moisture_local`：局部/接头受潮，低阻抗区边界软化，后方反射衰减。
  - `moisture_distributed`：长区段/整体受潮，从起点后持续下移或缓慢下降，末端电气位置可后移，后方反射减弱。
- 受潮段不再使用单个硬边界 `SegmentParams`；通过多个短子段模拟 `z0/epsr/alpha` 沿距离渐变。
- 随机 Field 生成中，旧 `moisture` 选择需映射为 `moisture_local` 或 `moisture_distributed`，YAML 记录明确类型。
- 长 Field 相频需要保留低频到中频的长度相关包裹周期；周期应由 `2 * length * sqrt(epsr) / c` 对应的传播延迟决定，不能用随机相位纹理伪造。
- 相频修正应通过低频相干反射/终端传播项实现，高频仍允许随损耗衰减；不得用任意全频相位旋转制造距离域虚假事件。
- 回看实测 Field CSV 后确认：实部、IFFT 脉冲和阶跃响应优先级高于相频外观；低频相干项只能作为弱扰动，不能主导 Re(S11) 包络或生成过强的理想远端脉冲。后续要提升实部真实性，应优先从夹具/校准残差/实测模板混合建模入手，而不是单独增强传播相位项。
- `--defect_case_preview` 不应继续输出纯净级联模型；手工缺陷 case 现在默认采用“固定缺陷拓扑 + dirty measurement background”，从 RG58/Field 实测 S11 与 Field 校准模板中采样背景污染。该预览用于检查缺陷在真实测量背景下的可见性，而不是展示理想物理响应。
- 进一步检查发现：长 Field 手工预览曾把 `termination="open"` 和 `z_load_open=85Ω` 混用，导致末端反射被错误削弱；已改为 Field 预览开路末端使用高阻抗。分布式受潮 case 禁止实测模板相位/慢基线覆盖，因为无标签实测模板会改写受潮阶跃趋势；该类 case 保留合成 dirty 噪声/夹具污染，但优先保持“受潮区间下沉、后方反射衰减、末端仍可见”的物理形态。

## 2026-06-23 分布式受潮伪脉冲修正记录

- 最新检查确认：当前场景暂不考虑受潮区间内部接头，因此 `moisture_distributed` 不应在 IFFT 脉冲响应中产生连续多峰；PDF 中的多峰来自中间接头，不应迁移到无接头受潮样本。
- 物理优先级固定为：`S11_Real`、IFFT 脉冲响应、IFFT 阶跃响应优先，其次才是幅频和包裹相频外观。任何相频/幅频修正都不能破坏距离域主事件。
- 分布式受潮的目标形态：
  - 阶跃响应从受潮起点后缓慢下降或整体下移。
  - 末端阶跃和末端脉冲反射减弱，但末端事件仍应可识别。
  - 受潮导致等效波速下降，末端电气位置可后移，响应尾部变长。
  - 脉冲响应内部不应出现由人为分段边界造成的一串周期性峰。
- 生成实现约束：
  - `moisture_local` 可以保留软边界短区段语义。
  - `moisture_distributed` 不再使用 6-18 个硬 `SegmentParams` 子段表示连续渐变；没有内部接头时，应采用单个连续异常介质段或极少数平滑过渡段，避免把介质渐变误建模成多个反射界面。
  - 不再叠加额外的解析散射/微散射项；这些项会造成阶跃局部上抬或内部脉冲污染。当前采用单个连续受潮介质段表达分布式受潮，靠低阻抗、`epsr` 增加和损耗升高形成阶跃下移与末端变弱。
  - YAML/label 仍记录一个受潮区间；标签是区间异常置信度，不代表区间内存在多个可反射缺陷点。
- 二次修正：手工 Field 缺陷 case 不能混入实测 S11 模板，因为模板中可能包含未知末端/接头事件，会遮蔽固定拓扑的缺陷和末端；Field healthy 可继续用模板作为背景参考，Field defect case 只保留受控 dirty 噪声、夹具和校准残差。
- 受潮阶跃验收改为强约束：`moisture_distributed` 的 200 MHz 阶跃在受潮早期不得高于受潮前基线，区间后段必须继续下降；不能用“先上升再末端下跌”冒充受潮缓慢下降。

## 2026-06-23 受潮慢阶跃与预览伪峰修正记录

- 本轮修复依据：受潮在当前无中间接头场景下不应表现为多个脉冲峰，也不应在阶跃中表现为硬突降；应表现为受潮起点后缓慢下移、末端反射/阶跃减弱、尾部变长。
- `moisture_distributed` 不再通过强低阻抗硬槽、多段湿区边界或额外离散散射来塑形；这些做法会分别导致阶跃突降、内部伪峰或中段上抬。
- 手工缺陷组图用于检查固定拓扑机理：Field defect case 禁止混入无标签实测模板，Field healthy 可继续使用模板展示真实背景复杂度。
- RG58 random 手工预览的未标注健康段只允许首端夹具/噪声级扰动，不能在健康段中出现缺陷级孤立峰；case 2/3/4 需要专门测试。
- 实施优先级：先保证 `S11_Real`、IFFT 脉冲和 IFFT 阶跃；幅频/相频只作为次级观测，不能通过相位纹理或模板残差制造距离域事件。

## 2026-06-24 第二批手工缺陷 case 组图

- 本轮手工组图改为第二批不重复 case，用于继续检查每类缺陷机理在不同长度、位置和组合下的表现，不再沿用上一批 11 个样例的长度、缺陷位置和组合信息。
- RG58 覆盖：已知 BNC/接头语义、短随机健康线、短随机单缺陷、较长随机双缺陷；长度和缺陷位置避开上一批 `74/120/180 m` 样例。
- Field 覆盖：短 Field 健康、近端短缺陷、远端短缺陷、`aging`、`moisture_local`、`moisture_distributed`、`short+moisture_local`、`short+moisture_distributed`。
- Field defect case 仍禁止混入无标签实测模板；Field healthy 仍可保留模板背景，作为真实背景复杂度参考。
- 组图仍输出 `S11_Real`、幅频、包裹相频、IFFT 脉冲、IFFT 阶跃五张图；验收优先级保持 `S11_Real`、脉冲、阶跃优先，幅频和相频次级。

## 2026-06-24 RG58 频域与长缺陷慢波速修正记录

- 保留当前阶跃响应形态作为硬约束，不通过强阻抗边界重塑受潮。
- RG58 预览需要恢复更真实的高频色散和宽频粗糙度，避免 `200-400 MHz` 出现过于人工的实部/幅值平台或缺口；RG58 高频衰减不能过早把色散抹平。
- `moisture_distributed` 应进入 S11 物理传播链路中的电气长度：湿区采用低反射的慢波速/增损耗介质段，阶跃慢下降仍由距离域连续算子控制，避免回到硬低阻抗槽。
- `aging` 和 `moisture_distributed` 的末端事件允许相对名义长度后移；后移量主要来自缺陷段 `epsr` 增大导致的有效传播速度下降。
- 分布式受潮 case 的低/中频包裹相位不能直接禁用相干保留项；相干项应按有效电气长度计算，并保持小幅度，不能制造新的距离域伪峰。

## 2026-06-24 第三批手工缺陷 case 组图

- 在保留当前 RG58 频域、分布式受潮慢波速、阶跃慢下降和相频保留修正的基础上，替换为第三批不重复 case。
- 第三批 case 不复用第二批的 `52/36/68/196/350/620/1250/1100/1280/1650/1900/2400 m` 长度，也不复用其缺陷位置组合。
- RG58 继续覆盖已知多 BNC、短健康线、单短缺陷和双短缺陷；Field 继续覆盖健康、短缺陷、aging、moisture_local、moisture_distributed 及组合。
- 输出仍复用 `Image/DG_defect_cases_*.png`，用于人工连续检查同一物理模型下的不同拓扑表现。

## 2026-06-25 长 Field 低频实部与长缺陷脉冲振铃修正记录

- 保留第三批 case 和当前阶跃响应作为硬约束；未被指出的问题不做重构。
- 第三批最后三个 Field 长电缆缺陷 case 没有实测模板背景，低频 `Re(S11)` 容易偏小；需要增加长度相关的低频相干实部保留项，让 `>1500 m` Field defect case 在低频仍有可见周期/包络。
- 低频增强只作用于 Field 长电缆低频段，不能通过全频增益或随机纹理破坏 IFFT 脉冲/阶跃。
- `aging` 属于长距离介质变化，不应像普通短缺陷一样用单个硬边界段表示；应改成渐变异常段，降低长缺陷边界造成的缺陷级脉冲振铃。
- `short` 仍保留局部阻抗突变语义，但手工 case 中过强的短缺陷参数应控制在可见而不过度振铃的范围内。
- 只读实测统计补充：`E:\FDR案例-csv\无校准S11` 中 `>1500 m` Field 样本的 `0-20 MHz |Re(S11)| p95` 中位约 `0.77`，`20-80 MHz |Re(S11)| p95` 中位约 `0.80`；第三批最后三个生成 case 仅约 `0.31-0.54` 和 `0.21-0.22`，因此需要按实测低频能量下半区做强约束。
- 本轮实现应把 `aging` 和 `moisture_local` 从普通硬边界 `is_defect=True` 段中分离，改为低反射传播段加区间元数据；`short` 仍保留普通阻抗突变段。

## 2026-06-27 Field 健康末端与分布式浸水后段阶跃修正记录

- 保留当前满意部分：RG58 频域、短缺陷可见性、浸水段内部慢下降、分布式浸水内部无连续脉冲峰、长 Field 低频实部增强。
- Field healthy 固定预览继续保留无标签实测模板背景，但模板背景不能覆盖健康电缆末端；`500-1200 m` 无缺陷 Field 需要受限末端锚定，让名义末端附近出现可解释的宽末端脉冲和阶跃上升。
- Field healthy 的末端前后模板事件需要限幅，避免出现“末端附近振荡团但无主峰”的健康样本；该约束只作用于无缺陷 Field，不处理 `short`、`aging`、`moisture_*` 缺陷区间。
- `moisture_distributed` 的浸水区间内部继续保持慢下降；但在浸水结束后，阶跃斜率应平滑恢复到接近浸水前正常段，不应继续维持浸水段内的下滑趋势。
- 分布式浸水末端仍应可识别：在 `effective_terminal_phase_length_m(cable)` 附近保留低幅、宽、平滑的末端阶跃上升，最后一个 `aging+moisture_distributed` case 也适用；对应脉冲不能变成短缺陷级尖峰。

## 2026-06-29 1500m 接头浸水实测约束校准记录

- 只读参考 `E:\FDR案例-csv\1500m电缆单个接头浸水试验`：正常样本与 100-1400 m 单接头浸水样本对比表明，浸水接头后的阶跃电平会偏移，但接头后健康段斜率基本回到同位置正常电缆斜率，不应继续保持浸水段内的下滑趋势。
- 本轮不新增 `joint_water_ingress` 标签类型；实测接头浸水只用于校准现有 `moisture_local` 和 `moisture_distributed` 的距离域形态。
- `moisture_local` 与 `moisture_distributed` 都进入统一的后段斜率恢复算子：受潮段内允许下沉或缓慢下降，`end_m` 之后用平滑迭代校正把后续健康段斜率拉回受潮前邻近健康段附近。
- 恢复算子只修正阶跃后段斜率，不在后段撤回电平偏移；末端反射窗口仍由末端锚定逻辑保护，避免把受潮恢复误做成末端伪峰或削弱真实末端。
- 回归约束新增 `moisture_local` 后段恢复测试，并保留 `moisture_distributed` 后段恢复、区间下沉、末端可见和无短缺陷级伪峰测试。
